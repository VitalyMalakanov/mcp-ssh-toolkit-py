# Example environment variables for mcp-ssh-toolkit-py

# Default SSH username (optional)
SSH_DEFAULT_USER=youruser

# Default SSH port (optional)
SSH_DEFAULT_PORT=22

# Default SSH timeout (optional)
SSH_DEFAULT_TIMEOUT=20
